#include <iostream>
#include "sortowanie.h"

using namespace std;

int main()
{
    /*int arr[] = {9,3,15,0,4,1,10};
    //bubbleSort(arr, sizeof(arr)/sizeof(arr[1]));
    //mergeSort(arr,0,5);
    //insertSort(arr,sizeof(arr)/sizeof(arr[1]));
    //selectionSort(arr, sizeof(arr)/sizeof(arr[1]));
    //quickSort(arr,0,6);
    heapSort(arr, 7);
    printArray(arr, sizeof(arr)/sizeof(arr[1]));
     */
    

    return 0;
}
