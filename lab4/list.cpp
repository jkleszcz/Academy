#include <iostream>

List::List(){

}

List::List(int const arr[], int size){
    Node *tmpNode;
    for(int i=0 ; i<size ; ++i){
        Node node;
        node.value;
        if(this.head != nullptr)
                *tmpNode.pNext = &node;
        else
            this.head = &node;
    }
}
